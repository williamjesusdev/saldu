const config = {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  tabWidth: 2,
  endOfLine: 'crlf',
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
